export const metadata = { title: 'VoiceTranslate', description: 'Real-time voice translator' };
export default function RootLayout({ children }) {
  return <html lang="en"><body style={{margin:0,padding:0}}>{children}</body></html>;
}
